#ifndef MAIN_H
#define MAIN_H

#include <unistd.h>
#include <stdarg.h>
#include <stdio.h>

int _putchar(char c);
int print_string(char * str);
int _printf(const char * format, ...);
#endif /* MAIN_H */
